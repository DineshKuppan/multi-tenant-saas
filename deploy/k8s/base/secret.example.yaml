# Template only — do not apply as-is or commit a filled-in copy.
#
# Create the real secret out-of-band instead, e.g.:
#   kubectl create secret generic api-secrets \
#     --from-literal=database-url="postgres://user:pass@host:5432/app?sslmode=require"
#
# In a real cluster, source this from a secrets manager (Vault, cloud
# provider secret store, etc.) via an external-secrets integration rather
# than kubectl create secret with a literal — see PROJECT_PLAN.md.
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
type: Opaque
stringData:
  database-url: "postgres://user:pass@host:5432/app?sslmode=require"
